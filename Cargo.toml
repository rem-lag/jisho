[package]
name = "jisho"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.11", features = ["blocking", "rustls-tls"], default-features = false }
scraper = "0.20"
colored = "2.0"
